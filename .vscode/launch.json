{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run UberRidePlatform",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/UberRidePlatform.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build UberRidePlatform"
        }
    ]
}
